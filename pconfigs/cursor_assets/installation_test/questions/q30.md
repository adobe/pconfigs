### Q30: pinputs in a pproperty

**Prompt:** A `SystemConfig` has a `module_config: ModuleConfig` field that the
user provides. Write a `@pproperty` on `SystemConfig` that overwrites
`module_config`, pinning its `checkpoint_dir` to `self.base_dir + '/checkpoints'`
while preserving all other user-provided values.
