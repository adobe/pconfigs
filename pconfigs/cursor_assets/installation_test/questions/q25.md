### Q25: Derived config field

**Prompt:** A `RunDirConfig` has an `output_dir: str` field that the user sets.
There's also a `log_dir` that should always be `output_dir + '/logs'` — it's
derived from `output_dir` and users should not set it independently. Implement RunDirConfig.
