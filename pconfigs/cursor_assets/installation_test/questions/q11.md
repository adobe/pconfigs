### Q11: Configurable torch.optim.Adam

**Prompt:** A colleague wants to use an optimizer, `torch.optim.Adam`, within their
trainer class, and they want to enable various experiments to change the `lr` and
`weight_decay` values. Show the code.
