### Q26: Adding gradient accumulation

**Prompt:** A `Trainer` class has a `train_step` method that calls
`optimizer.step()` every iteration. This trainer has been working well for many experiments, but you want to add 
optional gradient accumulation — accumulating gradients over N steps before calling `optimizer.step()`. Describe
how you would add this feature.
