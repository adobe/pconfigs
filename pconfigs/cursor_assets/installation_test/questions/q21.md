### Q21: Metric base class naming

**Prompt:** For a class that computes validation metrics on a validation dataset, I'm defining a helper class that 
computes a particular metric (e.g., mean squared error), and I'm creating a generic base class for that metric. 
A colleague suggests naming the base class `ValidationMetric`. Is this name correct? If not, what should it be? 
Explain your answer in 1-2 sentences.
