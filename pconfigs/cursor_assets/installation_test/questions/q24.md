### Q24: Configurable loss function

**Prompt:** A colleague has this trainer class:

```python
class Trainer:
    def compute_loss(self, predictions: Tensor, targets: Tensor) -> Tensor:
        return torch.nn.functional.l1_loss(predictions, targets, reduction="mean")
```

They want to make the loss function and its parameters configurable so different
experiments can try different loss functions (L1, MSE, smooth L1, etc.) with
different settings. Implement this.
