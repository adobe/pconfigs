### Q13: Full module with mode switching

**Prompt:** Write a complete `@pconfiged` class with config, defaults, and
an `__init__` that constructs a sub-config. Include a `mode` field that
switches behavior in a method. Show the full module.
