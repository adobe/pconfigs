### Q28: Making hardcoded values configurable

**Prompt:** A colleague has this class:

```python
class ImagePreprocessor:
    def __init__(self):
        self.normalizer = Normalizer(mean=0.5, std=0.2)

    def process(self, image: Tensor) -> Tensor:
        return self.normalizer(image)
```

They want `mean` and `std` to be configurable so different experiments can use
different normalization settings. Show the updated code.
