### Q02: Configurable DataReader class

**Prompt:** Create a new configurable class called `DataReader` that has a
`batch_size` and a `file_path` (the path to the data file to read). Show the full module.
