### Q09: Model selection by name

**Prompt:** Users need to select among several model classes at config time. A colleague
requests a registry dictionary that maps string names to model classes. Design the
model selection mechanism.
