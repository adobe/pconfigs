### Q19: Logger wrapper naming

**Prompt:** I need a logger wrapper class that only emits error-level messages, filtering out everything below error
severity. A colleague suggests naming it `MinimalLogger`. Is this correct? If not, what should it be? Explain in
1-2 sentences.
