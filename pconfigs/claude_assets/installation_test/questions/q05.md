### Q05: Sub-config field override

**Prompt:** I have a base config instance imported as `base`. I want to create
a new instance that changes only `sub_config.learning_rate` to 0.001. Show the
config instance file.
