### Q04: Sub-config usage across methods

**Prompt:** The `DataReaderConfig` has a field `transform_config: TransformConfig`.
Show the `DataReader.__init__` method and the `read` method that uses the transform.
