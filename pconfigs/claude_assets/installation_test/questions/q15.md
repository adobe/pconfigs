### Q15: Normalize method signature

**Prompt:** Add a method `normalize` to a `@pconfiged` class. It accepts an `image` tensor and a `mode` string
selecting the normalization strategy. Show the method signature and body.
